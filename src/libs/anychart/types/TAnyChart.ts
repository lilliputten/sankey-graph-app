export type TAnyChartRecord = unknown;

/* // TODO: Create precise data format description (when R&D's has finished).
 * export interface TAnyChartRecord {
 *   from: string;
 *   to: string;
 *   value: number;
 * }
 */

export type TAnyChartData = TAnyChartRecord[];
